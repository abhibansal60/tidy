# Python and SQLite

The application is Python (stdlib plus pinned Google libraries) with one SQLite file under `.jev/`. Old `rank.py` architecture and scoring are a baseline, not a constraint. A single-user, single-machine, owner-started tool needs no server or framework.
